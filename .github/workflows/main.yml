name: NEO Project CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  infrastructure:
    name: Terraform & Provisioning
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2 # أوهايو

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init & Plan
        run: |
          # البحث عن المجلد الذي يحتوي فعلياً على ملفات .tf
          # سنتخطى المجلدات الفارغة أو المخفية
          TF_DIR=$(find . -name "*.tf" -not -path "*/.*" -exec dirname {} + | sort -u | head -n 1)
          
          if [ -z "$TF_DIR" ]; then
            echo "❌ Error: No Terraform files (.tf) found in the repository!"
            exit 1
          fi
          
          echo "✅ Found Terraform configuration in: $TF_DIR"
          cd "$TF_DIR"
          terraform init
          terraform plan

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Python Check
        run: |
          # فحص السنتكس لكل ملفات البايثون المرفوعة
          find . -name "*.py" -exec python -m py_compile {} +
          echo "✅ Python syntax check passed."

      - name: Provision Infrastructure
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          REGION: us-east-2
        run: |
          # البحث عن السكربت وتشغيله
          SCRIPT_PATH=$(find . -name "provision-customer-updated.sh" | head -n 1)
          if [ -z "$SCRIPT_PATH" ]; then
            echo "❌ Provision script not found!"
            exit 1
          fi
          
          chmod +x "$SCRIPT_PATH"
          echo "🚀 Running Provisioning Script..."
          # تشغيل السكربت مع تمرير بيانات تجريبية
          ./"$SCRIPT_PATH" -d "deploy-test.com" -e "admin@neo.com" --dry-run
